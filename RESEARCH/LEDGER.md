# Experiment Ledger

Append-only. One line per round. The status report.

Schema (pipe-separated):
```
date | round | hypothesis-id | status | branch | mlflow_run | primary_metric_delta | one_line_note
```

`status` ∈ {accept, iterate, kill, bootstrap, housekeeper}.

---

2026-05-09 | 000 | H-bootstrap | bootstrap | agent/round-000-bootstrap | n/a | n/a | Loop infrastructure ported from barrier_classifier sibling: RESEARCH/ governance, scripts (loop_daemon.ps1, loop_status.py, critic_check.py, compact_loop_state.py, run_round.py, audit_chart.py), generic src modules (uncertainty.py, conformal.py, mlflow_utils.py, training_helpers.py, backtest.py), 24 architecture-agnostic tests passing. Project-specific tests parked in tests/_pending/ for re-enable as utilities land in H-101..H-103. BACKLOG re-anchored to offline+online system; H-101 (port labels+splits) is top priority.
2026-05-09 | 001 | H-005 | accept | agent/round-001-h005-first-pnl | barrier_round_001 | offline τ=0.30 Sharpe=+1.50 PSR=0.995 p_boot=0.000; final τ=0.20 Sharpe=-4.69 p_boot=0.99 | First PnL number for online_barrier_classifier. Glue-only round: load predictions.parquet + 1m + bars_20m, map open_time→minute_idx (CODE-SCOUT), drop bars whose [n_k+1,n_k+M] crosses segment gap, run simulate_inventory_aware on τ_open∈{0.10,0.15,0.20,0.30,0.50} for {p_offline,p_final=p_online} with symmetric φ=c_stop=α=0.0041113 and 1bp/side cost. Shuffled-signal random-entry null (n=200) per Bailey/Borwein/LdP 2014 — drift-matched, addresses BTC2024 +120% drift. Result: ALL metrics finite (primary falsifier passes). p_offline has p_boot<0.05 at every τ (best at τ=0.30 with PSR=0.995); p_final/p_online has no detectable edge above τ=0.10. Online layer's ROC-trade-for-coverage cost shows up as flat-or-worse Sharpe — consistent with CONSTITUTION I (online primary metric is coverage, not Sharpe). Followups: H-005b (val-split τ_open selection), H-005c (CSCV PBO post-hoc τ deflation, blocked on H-113). Loop infra fixes also in commit: critic_check uses master not main, causality test check tolerates _pending state, .gitignore excludes pycache, conformal property test slack 3σ→4σ.
2026-05-09 | 002 | H-201 | accept | agent/round-002-h201-coverage-baseline | n/a (no MLflow this round; deterministic measurement) | Mondrian LAC max\|gap\|@α=0.10 = +0.034 (within 5pp); naive-threshold max\|gap\|@α=0.10 = +0.099 (way over); Mondrian @α=0.20 low-vol gap = -7.9pp on p_online (the 'gap to close' for H-202..H-208) | Coverage baseline on existing ARF + offline predictions. Pure measurement round; no model retrain (seed_noise_band=0). Glue-only: load predictions.parquet + bars_20m_features tail, derive parkinson_var_rolling_mean_24 terciles, chronological 30/70 cal/eval split (n_cal=9446, n_eval=22040). For predictors {p_offline, p_online} × α∈{0.05,0.10,0.20} compute three modes: naive_threshold (set={y:p≥α}, no calibration; the 'as-if-conformal' baseline H-201 names), lac_marginal (split-conformal global q_hat per Manokhin Ch.3), lac_mondrian (per-tercile q_hat per Manokhin Ch.11). Plus constant-predictor sanity: at α=0.05 cov=1.0000 ✓, at α=0.20 cov=0.9181 = 1-eval_base_rate ✓ (harness-falsifier passes per THEORIST). Findings: (1) naive uncalibrated p≥α over-covers by 8-10pp at α=0.10 in every regime — confirming raw ARF probabilities are NOT directly usable as conformal thresholds. (2) marginal LAC has +3.3pp under-coverage on high-vol p_online @α=0.10 vs -7.9pp over-coverage on low-vol @α=0.20 — exact conditional miscalibration THEORIST predicted. (3) Mondrian LAC compresses the α=0.10 gap to ±3.4pp everywhere but at α=0.20 still has -7.9pp gap on low-vol/p_online and -5.8pp on low-vol/p_offline — these are the gaps H-202 ACI / H-203 Mondrian-ACI must close. Spawned: H-201b (finer regime binning at α=0.20 — quintiles on parkinson_var, see whether higher-resolution Mondrian closes the low-vol gap further), H-201c (compare gap at fixed α=0.10 across cal-fraction ∈ {20%,30%,40%,50%} to check sensitivity to cal-window size).
2026-05-09 | 003 | H-108 | accept | master | n/a (engineering port; no model training on real data) | port verified: 10/10 ensemble tests pass; on N=3 synthetic-fit seeds median seed-disagreement std=0.017, p95=0.036, max single-model deviation from ensemble = 0.070 → averaging is non-trivial | CatBoostEnsemble class ported from sibling barrier_classifier/src/utils.py to src/ensemble.py. Two extensions over sibling: load_ensemble(base_path, n_models) classmethod for the round-trip the autonomous loop needs (sibling has save but not load), plus __len__/n_models for ergonomics. Tests cover: empty-list rejection, len/n_models, predict_proba averaging matches manual mean (rtol=1e-10), proba rows sum to 1, predict thresholding monotone in τ, feature_importance averaging, get_best_iteration with eval_set, save+load roundtrip with all shards present, load_ensemble rejects n_models<1 and missing shards. Diagnostic plot RESEARCH/diagrams/round_003/ensemble_dispersion.png shows seed-disagreement histogram + scatter vs ensemble p — read back via Read tool, caption corrected after first render to match what data actually shows (no U-shape; broadly distributed). Unblocks H-206 (CatBoost virtual-ensemble σ_epistemic as conformal feature). No notebook integration in this round — that's a follow-up if needed; the class is drop-in compatible with the current `models[i].fit(...)` pattern in offline_train.ipynb.
2026-05-09 | 004 | H-101 | accept | master | n/a (engineering port; deterministic round-trip validation) | round-trip on persisted bars_20m_features.parquet (n=78,714): chronological_split utility produces train_end=37,783, val_end=47,228 — bit-identical to notebook cell-3; 21/21 label+split tests pass | Extracted label+split logic from notebooks/feature_build.ipynb cell 5 and notebooks/offline_train.ipynb cell 3 into src/utils.py: compute_log_excursion (next-bar high / current close, NaN at segment gaps), calibrate_alpha (90%-quantile of train-window excursions, with the notebook's `int(N*tf)-1` slice — the `-1` matters and is pinned by test), construct_labels (1[excursion ≥ α]), chronological_split (no embargo, no walk-forward — matches this project's contract), chronological_split_indices (helper exposing the boundary math; pins the notebook's two-step int() truncation `val_size = int(int(N*tf)*vf)` which differs from int(N*tf*vf) by ≤1). Tests pin: pointwise log_excursion on hand-checked 7-bar synthetic with one segment gap; gap-crossing label = NaN; series-end label = NaN; alpha calibration matches manual quantile; alpha rejects invalid train_fraction/quantile; split partition disjoint+ordered; split returns independent copies. Round-trip test loads the actual parquet and compares my utility's split sizes/boundaries against a verbatim re-implementation of cell 3 — match exact. Retired parked tests/_pending/test_causality.py + test_splits.py: their sibling-imported semantics (chronological_split_with_embargo, walk_forward_cv, minute-bar construct_labels with C/eta params, compute_past_target_features) don't match this project; replacement test file is tests/test_label_split_utils.py. Diagnostic plot RESEARCH/diagrams/round_004/split_window_visualization.png shows train/val/test on date axis with per-window p+ (0.097/0.113/0.097) and rolling positive rate by window — first render had corrupted x-axis (Binance microsecond timestamps mis-parsed as ns); fixed by routing through src.utils.infer_unix_timestamp_unit before re-rendering. Unblocks H-105 (NSGA-II HPO needs reusable splits).
2026-05-09 | 005 | H-106 | accept | master | n/a (engineering port + measurement on already-fit predictions) | offline ECE 0.050/0.103/0.171 (low/med/high parkinson_var_24 terciles); online ECE 0.015/0.014/0.016 (regime-flat). Global: offline ECE=0.108 Brier=0.090; online ECE=0.015 Brier=0.076. Direct empirical proof of CONSTITUTION I (online trades ranking for regime-conditional calibration). | Ported four helpers from sibling barrier_classifier/src/utils.py to this project's src/utils.py: expected_calibration_error (10-equal-width-bin, last-bin-closed-on-right, sklearn-compatible), compute_all_metrics (roc_auc + pr_auc + log_loss + brier + ECE), calibration_by_regime (pd.qcut(n_regimes) with default low/med/high labels for 3 regimes; min_samples_per_regime=50 drops noisy buckets; reports n_samples + base_rate + ECE + Brier + mean_predicted per regime), threshold_analysis (101-point sweep returning n_trades/trade_rate/precision/recall, monotone in trade_rate). 13 new tests in tests/test_calibration_metrics.py pass: ECE=0 on perfect calibration, ECE=|p−base_rate| on constant predictor, p=1.0 included in last bin (boundary case), compute_all_metrics returns documented keys all-finite, calibration_by_regime n_samples sum to N, undersized-bucket dropping, label/n_regimes mismatch rejected, threshold_analysis monotone-decreasing in n_trades, precision matches manual at sample threshold, NaN precision when n_trades=0. Diagnostic RESEARCH/diagrams/round_005/calibration_by_regime.png — 2×3 calibration curves on the actual test stream (n=31,486) with parkinson_var_rolling_mean_24 terciles. Top row (offline) shows systematic over-prediction at every regime, severity scaling with volatility (mean_p − base_rate: 0.072−0.022=0.050 in low, 0.366−0.195=0.171 in high); bottom row (online ARF) shows curves on the diagonal in every regime (mean_p ≈ base_rate per regime). Plus threshold_sweep.csv and headline.json with full per-regime metric blocks. Latent bug fixed: src/training_helpers.py:204 referenced bc_utils.compute_all_metrics which didn't exist — now does. Unblocks any round that wants regime-stratified accept-gates per CONSTITUTION IV.
2026-05-09 | 006 | H-107 | accept | master | n/a (engineering port; deterministic visual diagnostic) | 13/13 plotting smoke tests pass; visual report card on real data reproduces round-005 ECE numbers exactly (offline 0.108, online 0.015) — helpers wired correctly | Ported four plot helpers from sibling barrier_classifier/src/utils.py into a NEW src/plotting.py module (cleaner than sibling's all-in-utils arrangement): plot_calibration_curve (reliability diagram with ECE annotation; ax composable), plot_calibration_by_regime (side-by-side per-quantile; matches src.utils.calibration_by_regime semantics; n_regimes parameter beyond sibling's hardcoded 3), plot_feature_importance (API improved: takes importances ARRAY not model object → works with CatBoostEnsemble / sklearn / anything; rejects misaligned lengths; top_n auto-caps to array length), plot_threshold_curves (reads output of src.utils.threshold_analysis; rejects missing columns). Weight plotters (plot_weight_profiles, plot_weight_distributions) intentionally NOT ported — no sample weights in this project until H-102, would test against stub data which is worse than no test. Tests pin: each helper returns a usable Axes/Figure; titles + axis labels populated; ECE annotation toggleable; ax composability (caller passes their own ax); custom labels and n_regimes mismatch rejected. Diagnostic RESEARCH/diagrams/round_006/visual_report_card.png is a 2×2 grid (offline + online calibration curves; top-25/726 feature importance horizontal bars; threshold sweep) plus separate per-regime calibration_by_regime PNGs (offline + online) reproducing round-005's parity exactly. Top features by offline CatBoost importance: minute_sin (4.02), return_rolling_mean_8 (3.19), return_rolling_mean_12 (2.69), minute_cos (2.11), parkinson_var_rolling_mean_2 (1.28) — time-of-day cyclic + short-window momentum + Parkinson vol dominate the model. Helpers are now drop-in for offline_train.ipynb / online_eval.ipynb (notebook integration deferred — reusable in any future round).
2026-05-09 | 007 | H-202 | accept | master | n/a (deterministic measurement; no model retrain) | ACI marginal coverage on real stream (n_eval=22,040, γ=0.01): 0.9508 / 0.9018 / 0.8023 (p_offline, α=0.05/0.10/0.20); 0.9505 / 0.9023 / 0.8023 (p_online). Marginal gap to target ≤ 0.003 (≤ 1.5σ, σ≈0.002). G&C 2021 Thm 1 marginal-coverage falsifier PASSES. | First true online conformal layer in this project: aci_step (one-step LAC update, q_{t+1}=clip(q_t+γ(err_t-α), 0, 1), set construction matches batch predict_set) and aci_stream (drives aci_step over a binary stream, returns sets/q_history/err_history/coverage). 14 new tests in tests/test_aci.py pin: per-step math (covered → q decreases by γα; miscovered → q increases by γ(1-α)); γ=0 freezes q; q clamps to [0,1]; set construction is parity with predict_set at fixed q; rejects invalid α/γ/y; stream shape parity with input; q_history starts at q_init; coverage converges within 5σ on stationary IID stream; γ scales q-volatility; rejects shape mismatch. Diagnostic RESEARCH/diagrams/round_007/ has three figures: aci_q_trajectory.png (q_t for both predictors at α=0.10 over 22,040 steps; both swing 0.05-0.80, p_online noisier than p_offline due to score-distribution shifts), aci_coverage_convergence.png (cumulative coverage converges cleanly to the 0.90 target line by step ~5000 for both predictors), aci_per_regime_vs_round002.png (signed coverage gap, target-empirical, ACI vs round-002 Mondrian-LAC at α=0.10 and 0.20). Per-regime finding: ACI's single global q_t cannot match Mondrian's per-regime q_hat — at α=0.10 ACI has -6.4pp low-vol over-coverage and +5.9pp high-vol under-coverage on p_offline (Mondrian: +3.4pp / -0.5pp); at α=0.20 ACI gaps are -9.3/+9.6pp vs Mondrian -5.8/-1.0pp. Plain ACI matches ARF/lac_marginal-level performance (better than naive_threshold's 8-10pp gaps) but loses per-regime to Mondrian. This is the expected story under non-stationary regime drift and is exactly what H-203 (Mondrian-ACI hybrid: per-regime q_t with the same online update rule) is designed to close.
2026-05-09 | 008 | H-203 | accept | master | n/a (deterministic measurement; no model retrain) | Mondrian-ACI per-regime gap collapses to ≤0.6pp everywhere; beats round-002 Mondrian-LAC; the α=0.20 low-vol p_online gap that round-002 named as "the gap to close" closes from -7.9pp to +0.04pp. Marginal coverage stays on target. | Mondrian-ACI = plain-ACI lifted to per-regime q_t: aci_mondrian_step (looks up active regime's q, runs aci_step, returns updated regime->q dict; rejects unknown regime; doesn't mutate caller's dict) + aci_mondrian_stream (drives per-regime q over a stream with regime labels, returns sets/q_history/q_history_by_regime/err_history/coverage/regimes_seen). 9 new tests in tests/test_aci_mondrian.py pin: only-active-regime updates; returns independent dict (no caller mutation); rejects unknown regimes; **bit-exact reduction to plain aci_stream when n_regimes=1** (load-bearing); per-regime q_history lengths sum to T; per-regime coverage converges within 6σ on stationary IID stream; q_init_by_regime overrides default; rejects shape mismatch; integer regime labels work. Full suite: 113 passed. On real stream (n_cal=9,446 used per-regime to warm q_init, n_eval=22,040, γ=0.01, terciles fit on full df): plain ACI per-regime gap range ±[5.8pp, 10.4pp] → Mondrian-ACI per-regime gap range ±[0.04pp, 0.54pp]. Examples at α=0.10 / p_offline: low-vol -5.8pp → +0.04pp (Δ improvement 5.76pp); high-vol +6.2pp → -0.21pp (Δ 6.00pp). At α=0.20 / p_online (the canonical 'gap to close'): low-vol -4.1pp → +0.04pp; high-vol +7.4pp → -0.54pp. Comparison vs round-002 batch Mondrian-LAC: Mondrian-ACI BEATS Mondrian-LAC across the board — at α=0.10/p_offline Mondrian-LAC was +3.4pp low-vol/-0.5pp high-vol; Mondrian-ACI is +0.04pp/-0.21pp. The R002 baseline used a held-out cal set + batch quantile; Mondrian-ACI uses per-regime online updates with γ=0.01 and per-regime warmed q_init; the online formulation handles regime-specific score distribution drift better than a static per-regime quantile. Diagnostic RESEARCH/diagrams/round_008/three_way_gap_comparison.png shows ACI/Mondrian-ACI/Mondrian-LAC bars at α=0.10 and 0.20 — Mondrian-ACI bars sit on the 0 line, others scatter ±5-10pp. Per-regime q trajectories plot (per_regime_q_trajectories.png) shows Mondrian-ACI's per-regime q stays near regime-specific levels (low-vol q stays low, high-vol q stays high) while plain ACI's global q oscillates in a wider band. Falsifier "Mondrian-ACI per-regime gap ≤ 5pp on every regime at α=0.10" PASSES with massive headroom (max gap 0.21pp). H-204 (River ARF vs SRP vs HAT under coverage-as-metric) and H-205 (locally-weighted conformal) become the next per-regime refinements; H-207 (ADWIN drift detector triggers ACI threshold reset) is now most natural to add on top of Mondrian-ACI.
2026-05-09 | 009 | H-005b | accept | master | n/a (no model retrain; uses persisted model.cbm to predict on val) | val-chosen tau* = 0.44 (inside R001 grid {0.10..0.50}); test Sharpe at tau* = -0.089 (PSR=0.458, p_boot=0.000 vs shuffled-signal null mean -3.57, DSR=0.000 after 26-tau-grid deflation); R001's PSR=0.995 at τ=0.30 was a post-hoc artifact and is hereby DEMOTED to "exploratory only". | scripts/round_009_val_tau.py loads artifacts/offline_model/model.cbm (726-feature CatBoost; selected_features.json was top-120 by importance, NOT a training restriction) and predicts p_offline on the chronological val slice (n=9,445 from chronological_split utility, all kept after gap-filter). Val tau-grid sweep on {0.10, 0.12, …, 0.60} (26 points, step 0.02) using simulate_inventory_aware with the round-001 setup (φ=c_stop=α=0.0041113, M=20, cost_bps=1.0); pick winner by max val Sharpe with n_trades>=50 stability filter. tau* = 0.44 (val Sharpe +0.347, n_trades 1,292 — many τ in val show NEGATIVE Sharpe, the +0.347 peak at τ=0.44 is the only positive island). Apply tau* to test predictions (existing artifacts/online_eval/predictions.parquet, gap-filter-aligned): test n_trades=3,427, total log return -0.024, max drawdown 0.361, hit rate 0.525. Bootstrap null (n=200, shuffled-signal random-entry per Bailey-Borwein-LdP 2014, drift-matched): null Sharpe mean -3.57, std 0.60, p_boot=0.000 → observed -0.089 dominates the null (model has SOME information, beats random-at-same-trade-rate). Deflated Sharpe (Bailey-LdP 2014, n_trials=26, var_trial=val sweep variance, n_obs=3,427, skew=0, kurt=3): DSR=0.000 (no edge survives multiple-comparisons deflation), SR_star=2.68 (the threshold that 26 zero-skill trials would need to beat). Plots: val_tau_sweep.png (val Sharpe-vs-τ curve with red dashed line at tau*; most of the grid is negative — the +0.347 peak at 0.44 is narrow and noisy), test_at_val_tau_equity.png (cumulative log return oscillates around zero, hits -25% drawdown midway through test, recovers partially, ends near zero). Honest summary: model has discrimination (it beats random-entry-at-same-rate), no tradable alpha at this label / cost / barrier-strategy combination after honest τ selection. Plus loop-infra: scripts/build_report.py + RESEARCH/REPORT.md — canonical living dashboard that pulls from every round's headline.json each iteration; trading section leads (per user direction); regenerated by Phase 6 of every round per LOOP_DISCIPLINE.md. Spawns: H-024 (re-evaluate label/strategy coupling now that the round-001 alpha is gone — first-touch label or no-stop backtest could change the picture), H-025 (try sized entries based on Mondrian-ACI confidence — does the conformal layer + sizing rule unlock something the binary p≥τ rule can't?). Both queued; user direction will pick.
2026-05-09 | 011 | phase-A-r1 | accept | master | n/a (no model retrain; uses persisted model.cbm + ARFClassifier replay) | baseline_offline_tau τ*=0.44 test Sharpe=-0.089 n=3,427 p_boot=0.000 (reproduces R009 sanity floor); baseline_online_tau τ*=0.52 test Sharpe=-11.59 n=289 p_boot=1.000 (online clusters near base rate; τ=0.52 is deep tail, the few entries are bad); combined_avg_tau τ*=0.18 test Sharpe=-0.116 n=7,207 p_boot=0.000 (50/50 averaging hurts: more trades, slightly worse Sharpe than offline alone). DSR within-strategy=0 for all three. | MANDATE OVERRIDE supersedes BACKLOG: Phase A round 1 lands the unified prediction surface and the first 3 of 7 Phase-A strategies. New code: src/inference.py (RegimeCuts dataclass + fit_regime_cuts on TRAIN-ONLY parkinson_var_rolling_mean_24 terciles; warm_q_init_by_regime per α from val LAC quantiles; predict() returning the §A.1 surface — p_offline, p_online, regime_id, q_lo_{05,10,20}, in_set_{05,10,20}, sigma_epistemic, confidence_score), src/strategies.py (StrategyOutput dataclass + 3 round-1 strategies in registry — baseline_offline_tau, baseline_online_tau, combined_avg_tau). 13 inference tests + 16 strategy tests pin: regime cuts boundary semantics + balanced fit; warm_q_init returns finite q in [0,1] with fallback for under-sampled regimes; predict() emits all mandated columns with finite q histories, in_set ∈ {0,1}, non-negative confidence; strategy registry contracts. River had numpy-2 ABI mismatch (river 0.21 → 0.24.2 fixes it). Round runner scripts/phase_A_round_011.py: chronological-split (train/val/test = 37,783/9,445/31,486); replay ARFClassifier prequentially over (last 96 train bars warmup) + val to materialise val p_online (n=9,445 cached at artifacts/phase_A/val_predictions_with_online.parquet); fit regime terciles on train (edges 2.86e-06 / 7.50e-06); warm Mondrian-ACI per-regime q on val LAC at α∈{0.05,0.10,0.20}; build unified val + test prediction tables via predict(); for each of the 3 strategies sweep τ∈{0.10..0.60 step 0.02} on val Sharpe with min_n_trades=50, evaluate at val-chosen τ on test, run shuffled-signal bootstrap null (n=200), within-strategy DSR. Diagrams under RESEARCH/diagrams/phase_A/round_011/: equity_overlay_baselines.png shows green (offline) hovering near 0, blue (online, almost no trades) drifting to -20%, red (combined-avg, 7k trades) hitting -42% drawdown around bar 12.5k. Reproduction: baseline_offline_tau hits R009's exact numbers (τ*=0.44, test Sharpe=-0.089, n=3,427) — the harness is bit-equivalent. Cross-strategy DSR (with proper n_trials = 3 strategies × 26 τ-grid = 78) deferred to round 013 when all 7 strategies are in. Wall clock: 285s. Phase A round 2 (strategies 4-7) is next.
2026-05-09 | 012 | phase-A-r2 | accept | master | n/a (no model retrain; uses round-011 unified prediction parquets) | All 7 §A.2 strategies under unified harness with 5-fold walk-forward + multi-strategy DSR (n_trials=137, var_trial=9.40). All strategies' multi-strategy DSR=0; strongest test Sharpe is offline_tau (-0.089). Stacked val Sharpe (-0.016) was best on val but stacker over-fits: test Sharpe collapses to -1.07. Conformal gate p_boot=0.39 (noise region). Mondrian-ACI sized k*=20 p_boot=0.49 (no edge). Walk-forward bars: offline mean +3.53±6.40 — std bigger than mean → not statistically distinguishable from zero across folds. | scripts/phase_A_round_012.py reads round-011's val/test unified prediction parquets and runs the remaining strategies (combined_stacked_tau via fit_stacker on val LR; conformal_gate_tau gates open at α=0.10 in_set; mondrian_aci_size sizes by clip(k*confidence_score) at α=0.10; null_random_at_rate at offline-baseline's empirical 0.123 entry rate, single-point grid). Adds to src/backtest.py: simulate_inventory_aware_sized (cost on full position, PnL × size, 7 tests including unit-size-equivalence + half-size-halves-pnl + zero-size-skips + size out-of-range rejection + inventory-cap preserved + c_stop=∞ disables SL); walk_forward_backtest (5 contiguous folds default; 4 tests); cscv_pbo (López de Prado 2014 Ch.11 PBO via combinatorial OOS rank logits; 4 tests including pure-noise PBO band 0.35..0.75 and clear-winner PBO<0.05). Adds to src/strategies.py: combined_stacked_tau + fit_stacker (sklearn LogisticRegression on val), conformal_gate_tau (in_set_{α} gate with α-level switchable), mondrian_aci_size (size = clip(k * max(0, p_off - (1-q_lo)), 0, 1)), null_random_at_rate (deterministic seed; rejects out-of-range target_rate). 169 tests passing; sklearn deprecation warning is library-internal (scipy L-BFGS-B disp/iprint deprecation in scipy 1.18). Fitted stacker: coef_offline=4.42, coef_online=1.16, intercept=-3.57 — heavily weights offline; online's marginal contribution (1.16) is small but positive. The val Sharpe peaking at -0.016 for the stacked model and then test Sharpe collapsing to -1.07 is the textbook overfitting signal. Diagrams under RESEARCH/diagrams/phase_A/round_012/: equity_overlay_round2.png (7 lines, all negative or near-zero on test, online crashes by bar 8k), walk_forward_sharpe_bars.png (mean ± std bars; std > mean for every non-null model), val_tau_sweep_round2.png (val sweep across knob axes). Phase A round 3 (CSCV pass + report) is next. Wall clock: 167s.
2026-05-09 | 014 | planning-014 | infra | master | n/a (research-planning round; no code shipped) | 13 new H-IDs (H-301..H-313, H-320-a, H-320-b) + 15 inline refinements; 3 genuine gaps surfaced (rolling-retrain ×2 = H-310; trade postmortem = H-306; per-metric bootstrap ×2 = H-320); 9 web-only references catalogued in INDEX.md under new [STATS-CI] tag. Synthesis: RESEARCH/research_plan_round_014.md with 8-ask→H-ID map. | *Refinement (round-015)*: this round's anti-slop section cited round-011 / round-012 numbers built under the wrong (sibling-averaging / sibling-stacking) framing. Both rounds were SUPERSEDED-by-015; their numbers are no longer canonical. The H-302 / H-304 / H-310-b card addenda referenced "the unified Phase A harness" — that harness was rebuilt in round-015 with 5 valid strategies (no `combined_avg_tau`, no `combined_stacked_tau`, Mondrian-ACI driven by p_online). Where round-014 cards mention "combined_*", read "deleted under round-015 cleanup". Next-8-rounds ordering remains valid (H-024 → H-320-a → H-320-b → H-310-a → H-310-b → H-306 → H-302 → H-305) but H-024 now runs against the round-015 harness, not the deleted ones.
2026-05-09 | 015 | phase-A-corrective | accept | master | n/a (no model retrain; reuses persisted offline model + cached val ARF replay) | Five legitimate strategies under the two-layer architecture (offline → online consumes p_offline → p_online is system output; Mondrian-ACI on p_online). baseline_offline_tau τ*=0.44 test Sharpe=-0.089 n=3,427 (R009 sanity floor reproduced exactly); baseline_online_tau τ*=0.52 test Sharpe=-11.59 n=289 p_boot=1.000; conformal_gate_tau τ*=0.52 test Sharpe=-11.87 n=288 p_boot=0.735; mondrian_aci_size k*=20 test Sharpe=-5.67 n=2,372 p_boot=0.505; null_random_at_rate r=0.123 test Sharpe=-3.96. Multi-strategy DSR=0 across all 5 (n_trials=85, var_trial_pooled=8.36). Cross-strategy CSCV PBO=0.000, modal IS-best=baseline_offline_tau across 12,870 combinations. Per-strategy PBO ≤ 0.003 — the val-chosen knob does NOT generalise worse than median OOS (knob landscapes are mostly monotone). | scripts/phase_A_round_015.py supersedes 011/012/013. Code changes: src/strategies.py: deleted combined_avg_tau, combined_stacked_tau, fit_stacker (architecturally invalid — averaged or stacked p_offline+p_online as if siblings); registry now contains exactly the 5 legitimate strategies (baseline_offline_tau, baseline_online_tau, conformal_gate_tau, mondrian_aci_size, null_random_at_rate). conformal_gate_tau and mondrian_aci_size now consume p_online (not p_offline) — round-008 contract restored. src/inference.py: predict() now drives aci_mondrian_stream off p_online; raises KeyError if p_online absent (architectural-precondition test pinned). Tests: 171 pass; tests/test_strategies.py adds `test_registry_rejects_sibling_combiners` to pin the architectural contract; tests/test_inference.py adds `test_predict_requires_p_online_driven_two_layer_architecture` and `test_predict_in_set_uses_p_online_not_p_offline`. Deleted artefacts: RESEARCH/diagrams/phase_A/round_{011,012,013}/, RESEARCH/diagrams/round_010/, scripts/phase_A_round_{011,012,013}.py, scripts/round_010_no_stop_backtest.py, artifacts/phase_A/{val,test}_predictions_unified.parquet (rebuilt with p_online-driven Mondrian-ACI), .tmp/round_{011,012,013}.log. KILL_LIST entries added for rounds 010/011/012/013. CONSTITUTION Section 0 strengthened with explicit "WHAT THIS IS NOT" block forbidding sibling-style averaging/stacking and offline-driven conformal. Diagrams under RESEARCH/diagrams/phase_A/round_015/: equity_overlay.png (green offline hovers near 0; purple/navy gates and online crash to -20%; teal mondrian_aci_size sized strategy hits -90% — sizing on a no-edge base amplifies losses; gray null drifts to -80%), val_knob_sweep.png, pbo_panel.png. Phase A backtest report at phase_A_backtest_report.md replaces all prior round-NNN/headline.json fragments. REPORT_LATEST.md DELETED — REPORT.md is the single living dashboard ("one truth"). REPORT.md fully rewritten around the corrected architecture. research_plan_round_014.md anti-slop section updated with round-015 numbers. Honest verdict: the two-layer system as currently fit does NOT produce tradable alpha — the offline layer alone is the best of the 5 strategies (modal IS-best across CSCV) and even it has Sharpe -0.089 below the deflation bar. The conformal layer gates and sizing scheme actively destroy value vs. offline alone (-11.87 / -5.67 vs -0.089). Consistent with CONSTITUTION I (online trades ranking for coverage) — round-008's regime-conditional coverage is achieved (≤ 0.6pp gap) but does not translate into tradable edge under the current label/cost setup. The genuine gaps surfaced in round-014 (rolling-retrain H-310, trade postmortem H-306, per-metric bootstrap H-320) are now the next moves. Wall clock: 47s (cache hit on val ARF replay).
2026-05-09 | 016 | planning-016 | housekeeper | master | n/a (research-planning round; no code shipped) | 19 new H-IDs (H-130, H-131, H-132, H-150, H-151, H-152, H-153, H-160, H-161, H-170, H-171, H-180, H-181, H-182, H-190, H-191, H-192, H-193, H-209, H-210) + 14 in-place refinements (H-040, H-115, H-114→H-131, H-102, H-020, H-024, H-025, H-204, H-206, H-207, H-208, H-022→H-161, H-023→H-160, H-113→H-192). 3 sub-agent corrections folded in: (a) Lekeufack §V-C miscitation — `(p − q̂)/(1 − q̂)` confidence-margin sizing is from Vovk-Gammerman-Shafer 2005 / Angelopoulos-Bates 2021 §2.2, NOT Lekeufack; (b) Multiscale Stochastic Volatility book has NO BPV chapter — that book is option pricing under multi-scale OU; BPV references must point to Barndorff-Nielsen & Shephard 2004; (c) H-161 meta-model architectural pin — input is `(features ⊕ p_online)` ONLY, `p_offline` strictly absent (round-015 contract). | MANDATE OVERRIDE: 8-axis re-anchor of the loop after rounds 010-015. Output: RESEARCH/research_plan_round_016.md (300 lines, ≤ 300 line cap met) synthesizing the 8 asks → 19 H-IDs map, four-phase round ordering (Phase A rounds 017-019 strategy escalation; Phase B 020-023 bootstrap + production refactor; Phase C 024-028 online ensembling + drift retrain; Phase D 029-034 features + postmortem). Reconciles with round-014 H-3xx series (kept; H-1xx are not duplicates — see §3 of plan doc). RESEARCH/BACKLOG.md grows to ~830 lines with new Tier 7 "Round-016 mandate cards" section organized by ask. RESEARCH/literature/INDEX.md adds 17 web-only references across new sections: [CALIB-UQ] (Vovk-Gammerman-Shafer 2005, Angelopoulos-Bates 2021), [VOL] (Barndorff-Nielsen-Shephard 2004 BPV, Barndorff-Nielsen-Kinnebrock-Shephard 2010 semivar, Patton-Sheppard 2015 good/bad vol, Cont 2001 stylized facts), [MICROSTRUCT] (Bieganowski-Slepaczuk 2026), NEW [CROSS-ASSET] tag (Liu-Tsyvinski 2021, Alexander-Heck-Kaeck 2022), [STATS-CI] (Künsch 1989 block-bootstrap consistency, Lahiri 2003, Bailey-Borwein-LdP-Zhu 2014 PBO), [FIN-ML] (Hudson-Thames blog, mlfinlab GitHub, nkonts/barrier-method GitHub), [ONLINE] (Wolpert 1992 stacking, Ting-Witten 1999, Pesaranghader 2017, Raab-2020 KSWIN, Raftery-2005 BMA, gooweml GitHub), [BACKTEST] (Qlib walk-forward, SHAP docs). Critical correction in INDEX: Multiscale Stochastic Volatility book (Best 10 #8) clarified as NOT a BPV reference — book is option pricing, no realized-variance chapter. Lekeufack INDEX entry updated with citation correction. CRITIC self-review confirms (a) no algorithm code added (zero src/ touched, zero compute_* added, zero notebook touched), (b) no causality invariants touched (CONSTITUTION I/§0 untouched), (c) all 19 new cards conform to ROUND_TEMPLATE.md format (Owner / Asks / Mechanism / Predicted effect / Falsification / References / Status / Cost / Dependencies), (d) no card averages or stacks p_offline and p_online — H-161 meta-model architectural pin makes this explicit. One commit, scope `loop-infra:`, no round-NNN-accepted tag (planning round per LOOP_DISCIPLINE.md). Halt-discipline: this round does NOT schedule a wakeup (no autonomous loop active); user picks next move from Phase A round 017 = H-024 unified no-stop variant on round-015 harness.
2026-05-09 | 017 | H-024 | accept | master | n/a (no model retrain; reuses round-015 unified prediction parquets; only c_stop=alpha → c_stop=inf) | **H-024 falsifier PASSES**: at val-chosen τ\*=0.20 under c_stop=inf, baseline_offline_tau test Sharpe = **+0.622** (vs round-015's −0.089 at τ\*=0.44 / c_stop=alpha) — ΔSharpe = +0.711, p_boot=0.000 (model has signal vs random-entry-at-rate), n=7,394 trades, hit-rate 0.257 (TP/total). All 4 mandate-required H-024 falsifier conditions met (Sharpe>0.5 ✓, p_boot<0.05 ✓). However: multi-strategy DSR=0 across all 5 (var_trial_pooled=16.869 inflated by failing strategies' val sweeps), within-strategy DSR=0 (the 26-tau val grid has high Sharpe variance). Cross-strategy CSCV PBO=0.000, modal IS-best=baseline_offline_tau across 12,870 combinations. WF Sharpe=+1.670±2.973 (std > mean → 5-fold CI crosses zero). Other strategies all degrade further with no-stop: baseline_online_tau (−2.20), conformal_gate_tau (−5.28), mondrian_aci_size (−5.17), null_random_at_rate (−3.01). | scripts/round_017_h024_no_stop.py: minimal Phase A re-run reusing round-015 cached unified prediction parquets (artifacts/phase_A/{val,test}_predictions_unified.parquet), only swapping c_stop=ALPHA → c_stop=float("inf") in {sweep_knob, shuffled_null, strategy_returns, test eval, walk_forward}. No retrain, no model touched. Outputs under RESEARCH/diagrams/phase_A/round_017/: phase_A_table.csv (5 rows), val_sweep_<strategy>.csv × 5, val_knob_sweep.png, equity_overlay.png (green offline rises to +0.5 cumulative log return; all others descend to -1.0..-1.7), diff_vs_round_015.csv + diff_vs_round_015_bars.png (offline strategy +0.71 ΔSharpe is the largest), cscv_per_strategy.csv (within-strategy PBO ≤ 0.012 for all), cscv_cross_strategy.json (cross-PBO=0.000), walk_forward_per_strategy.csv, headline.json. 171 tests pass; no src/ touched. **Interpretation**: round-015's null result was at least partly an artefact of the symmetric stop-loss clipping the offline layer's positive signal. The label/backtest mismatch IS a binding constraint per H-304 §5 Ask 5 prioritisation. The +0.622 result is NOT yet tradable alpha (DSR=0 after deflation) but is a clear directional improvement and the architecture IS doing useful discrimination. Per override §5 phase ordering: round 017 positive → round 018 = H-209 Cantelli sized + H-25 sized variants. Wall: 333.6s.
2026-05-09 | 018 | H-209 | iterate | master | n/a (UQ from single-seed virtual ensemble inadequate; queued for 3-seed retrain) | **H-209 falsifier triggered** in the configuration tested (single-seed CatBoost virtual-ensemble σ): val k\*=0 wins (val Sharpe -5.33), test Sharpe at k\*=0 = -5.55, ΔvsR017 binary baseline = -6.17, p_boot=0.000 (worse-than-shuffled-null), DSR_within=0.000. Three diagnostic root causes surfaced: (i) `|p_offline_cached - p_mean_vens| max = 0.40` — virtual-ensemble mean differs hugely from full predict_proba (n=10 sub-ensembles each have ~62 trees vs full 625). (ii) σ_epistemic mean = 0.47 — much higher than p_offline mean 0.21 — Cantelli `p_lower = max(0, p − k·σ)` collapses to 0 for most bars at k > 0.5. (iii) per-σ-decile mean-PnL is **inverse-monotone** (low-σ trades have worst PnL); single-seed σ from SGLB boosting trajectory is anti-informative for trade selection on this data. (iv) cost-on-full-position model (per backtest mandate §A.3) penalises sized-down strategies. **The proper H-209 test requires a 3-seed CatBoostEnsemble per BACKLOG spec**; queued as H-209-v2. Round 019 H-210 (virtual-ensemble agreement gate) skipped for the same reason; queued as H-210-v2. Pivoting to Phase B Round 020 H-190 (stationary block bootstrap) which is UQ-independent. | scripts/round_018_h209_cantelli.py: loads cached val/test unified prediction parquets + persisted offline CatBoost; computes σ_epistemic via `predict_with_decomposed_uq(model, X, n_virtual=10)`; Cantelli sized strategy `size = clip(p_lower − τ, 0, 1)` with `p_lower = max(0, p − k·σ)`; sweeps k ∈ {0, 0.5, 1, 1.5, 2} on val Sharpe with c_stop=∞ and tau=0.20 (round-017 winner); applies val-chosen k\* to test; bootstrap null + within-grid DSR + walk-forward + CSCV. Outputs under RESEARCH/diagrams/phase_A/round_018/: val_sweep_k.csv, val_k_sweep.png, test_equity.png, sigma_vs_pnl.png (the inverse-monotone per-decile chart that's the kill diagnostic), sigma_decile_pnl.csv, headline.json. UQ caches under artifacts/phase_A/{val,test}_sigma_epistemic.parquet (preserved for future H-209-v2 / H-302 work — even with 3-seed retrain, these are useful comparison reference). 171 tests pass; no src/ touched. KILL_LIST entry appended: round-018 H-209 single-seed iterate; round-019 H-210 single-seed skipped. Wall: 44.4s.
2026-05-09 | 020 | H-190+H-191+H-193 | accept | master | n/a (no model retrain; bootstrap library + CI audit) | **Audit downgrades H-024 to "exploratory only"**: round-017's headline +0.622 annualized Sharpe corresponds to per-bar Sharpe +0.0078 with 95% stationary-block-bootstrap CI [-0.0037, +0.0192] — **CONTAINS ZERO**. The four other strategies' CIs all firmly exclude zero (all negative). H-191 ROC-AUC DeLong CIs: p_offline=0.8130 [0.8054, 0.8207]; p_online=0.7988 [0.7906, 0.8069] — both tight, the offline-vs-online gap (-1.4pp) does not survive (CI overlap). H-191 PR-AUC stratified: p_offline=0.356 [0.341, 0.374]; p_online=0.331 [0.317, 0.348]. H-191 per-bin Wilson calibration confirms round-005 ECE thesis at 95% CI on every bin: p_offline systematically below diagonal at all 10 bins (over-predicting); p_online tracks diagonal within Wilson CI in 8/10 bins. H-193 sequential vs naive trade-bootstrap: ū=1.000 for all 5 strategies (the inventory cap in `simulate_inventory_aware_sized` enforces no-overlap by construction; sequential ≈ naive in this harness — H-193 is a no-op until a strategy permits overlapping positions). | scripts/round_020_bootstrap_audit.py + src/bootstrap.py (NEW MODULE, 470 lines) + tests/test_bootstrap.py (30 tests, all pass). Library implements: optimal_block_length (Politis-White 2004 flat-top plug-in with Patton-Politis-White 2009 correction), stationary_block_bootstrap_indices (Politis-Romano 1994), bootstrap_metric/sharpe/sortino/max_drawdown, wilson_interval (matches statsmodels.stats.proportion.proportion_confint to 1e-12 — pinned by test), delong_roc_auc_ci (Sun & Xu 2014 mid-rank O((n+m) log(n+m))), stratified_bootstrap_pr_auc (Boyd-Eng-Page 2013), trade_concurrency + average_uniqueness (AFML Ch. 4.4), sequential_bootstrap_indices + bootstrap_trade_metric_overlap_aware (AFML Snippet 4.5). Tests pin the falsifiers: IID-block-CI matches IID-bootstrap-CI within 10% width; AR(1) rho=0.7 widens correctly; DeLong half-CI vs IID-bootstrap diff <0.005 on synthetic balanced n=5000; full-overlap uniqueness = 1/3; no-overlap uniqueness = 1.0. Outputs under RESEARCH/diagrams/round_020/: h190_stationary_block_sharpe.csv, h190_sharpe_cis.png (clear visual: offline grey-CI straddles zero; all 4 others green-CI firmly negative), h191_auc_cis.csv, h191_calibration_wilson.csv, h191_auc_calibration.png (calibration curves with per-bin Wilson + AUCs with DeLong/stratified CIs), h193_sequential_trade_bootstrap.csv, h193_trade_bootstrap_widths.png, headline.json. 201 tests pass (171 prior + 30 new). **Major audit consequence**: round-017's H-024 falsifier "passed" with point Sharpe +0.622 but proper bootstrap CI on n=7,394 trades shows the signal is not statistically distinguishable from zero. This is exactly the Bailey-Borwein PSR=0.995 → DSR=0 demotion treatment from round-009, applied at the bootstrap-CI level. Round-017's H-024 status is now "exploratory only — directional improvement vs round-015 confirmed, but per-bar Sharpe CI contains zero". Wall: 163.5s.
2026-05-09 | 021 | H-180 | accept | master | n/a | Per-trade attribution dataframe with 34,854 rows × 6 strategies. **Diagnostic findings (informs Phase D)**: (i) H-209 σ_epistemic IS informative — top-σ-decile mean PnL is POSITIVE while bottom-σ-decile is NEGATIVE for every strategy; the round-018 single-seed UQ kill was correct *for sizing*, but σ as a *gating* signal works in the opposite direction (high-σ = more profitable, not less). (ii) baseline_offline_tau hourly Sharpe shows clear hour-of-day clusters: positive in hours 4-5 UTC and 21-22 UTC (+5 to +8 bp/trade), negative in 0-2 and 16-19 (-3 to -5 bp). (iii) per-regime mean PnL: baseline_offline_tau positive in low-vol regime 0 (+1.5 bp), neutral in regime 1, slightly negative in high-vol regime 2 (-1.5 bp). (iv) conformal_gate_tau and mondrian_aci_size have higher hit-rate (42.4% TP vs offline's 25.7%) but trade much less and have worse aggregate Sharpe — gating selects high-conviction trades but the higher hit-rate is not enough to overcome cost. (v) null_random_at_rate has TP fraction 10.2% — close to the 9.7% base rate, validating the no-skill comparator. | scripts/round_021_h180_per_trade.py: wraps `simulate_inventory_aware_sized` + cached test predictions/sigma to enrich each Trade with entry_p_offline, entry_p_online, entry_sigma_epistemic, entry_q_lo_{05,10,20}, entry_in_set_{05,10,20}, entry_regime, realized_pnl_bp, cost_paid_bp, entry_ts, entry_hour_utc, entry_dow. 5 round-017 strategies + h209_cantelli_k0 (round-018 sized variant) = 6 strategies. Outputs under RESEARCH/diagrams/round_021/: trade_postmortem.parquet (34,854 rows × 21 cols — substrate per H-306 spawning rule), postmortem_summary.csv (6 rows × 16 cols of slice summary), hourly_pnl.png (clean hour-of-day signal — `baseline_offline_tau` peaks ~hour 4-5 UTC), postmortem_panel.png (4-up panel; legend rendering is messy but data in CSV is clean), headline.json. 201 tests pass; no src/ touched. Wall: 2.4s.
2026-05-09 | 022 | H-040+H-115+H-131 (H-130 deferred) | accept | master | n/a (compute_* + smoke-on-real-data; no model retrain) | New module src/features.py (470 LoC, 9 compute_* functions) + tests/test_features.py (29 tests, all pass). Functions: compute_hurst_rs (R/S analysis), compute_dfa_alpha (Peng et al. 1994), compute_sample_entropy (Richman & Moorman 2000), compute_permutation_entropy (Bandt-Pompe with stable mergesort tie-breaking), compute_taker_buy_ratio + compute_signed_dollar_flow (H-130), compute_bpv_ratio (Barndorff-Nielsen & Shephard 2004), compute_signed_semivariance (Barndorff-Nielsen-Kinnebrock-Shephard 2010), compute_vol_of_vol. **Empirical findings on real BTCUSDT 20m test slice (n=31,486)**: (i) H-131 sv_up_w24 has Pearson 0.185 / Spearman 0.224 with label - strongest of new features and meaningfully informative. sv_up_w12 = 0.170, sv_up_w48 = 0.174. sv_down_w12 = 0.150 (also positive - high vol means both directions). (ii) sv_asymmetry_w12/24/48 are negative (-0.05 to -0.06) - surprising: realized SV asymmetry anti-predicts future upper hit (mean-reversion of momentum). (iii) bpv_rv_ratio_w12 = -0.056 - when no jumps (continuous regime), label less likely 1; jumps drive barrier hits. (iv) hurst_rs_128 = 0.042, dfa_alpha_128 = 0.022, permen_128 = 0.001 - all H-040/H-115 features are weak. (v) **CRITICAL**: sv_up_w24 has Pearson 0.86 with existing parkinson_var_rolling_mean_24 - most of the SV signal is already captured by existing features. Net incremental information likely small. (vi) **H-130 deferred**: taker_buy_base / taker_buy_quote columns ARE in the 1m kline  but were NOT aggregated to 20m bars in . The H-130 compute_* functions land tested but cannot run on the current bars_20m_features.parquet. Queued: H-130-prep round adds the aggregation. | scripts/round_022_features.py: smoke-runs all 9 functions on real data, computes per-feature label correlation + pairwise correlation with existing top-importance features. Wall: 395s (Hurst R/S took 352s due to pure-Python loop - would need vectorization for production). Outputs under RESEARCH/diagrams/round_022/: feature_distributions.csv, feature_distributions.png, pairwise_corr_with_existing.csv, headline.json. 230 tests pass (201 prior + 29 new). No model retrain; the proper feature-impact test (paired offline retrain DeltaBSS) is queued for a future round. **Recommended next feature work**: focus on directly-aligned features that don't duplicate parkinson_var; SV asymmetry already showed mean-reversion. The H-302 sigma-as-feature work from round-018 would also benefit from the proper 3-seed CatBoost ensemble. - H-130 raw data exists; aggregation is next round's investment.
2026-05-09 | 023 | H-130 | accept | master | n/a (data-prep + flow compute on real data) | H-130 prep round: aggregated taker_buy_base, taker_buy_quote, volume, quote_volume from 1m kline to 20m via cumulative-sum range-summing per CONSTITUTION I.6 (bar k uses minute bars [k*M, (k+1)*M)). Saved to data/model_data/BTCUSDT/bars_20m_flow.parquet (78,714 × 5 cols, ~3MB). Aggregation took 0.2s. **H-130 empirical findings on test slice (n=31,486)**: (i) flow_taker_buy_ratio_abs_w96 has Pearson -0.081 with label - most negative correlation among new features. High recent absolute flow correlates with LESS barrier hit (counterintuitive but consistent: large flow imbalance often precedes mean-reversion). (ii) flow_taker_buy_ratio_abs_w24 = -0.078 - same pattern. (iii) flow_signed_dollar_w12 has Pearson 0.63 with return_rolling_mean_12 - flow is highly redundant with rolling returns. (iv) Best new H-130 feature has Pearson -0.081 vs label - weak signal, weaker than H-131 best (sv_up_w24 = +0.185). (v) Net assessment: H-130 features add modest information; the abs-ratio signal is novel but small. | scripts/round_023_h130_flow.py: aggregate_flow_1m_to_20m via numpy cumsum + searchsorted; computes 14 H-130 features (8 ratio + 6 signed-dollar variants); reports per-feature label correlation + pairwise corr with existing top features. Outputs under RESEARCH/diagrams/round_023/: h130_feature_summary.csv, h130_feature_distributions.png, pairwise_corr_with_existing.csv, headline.json. Plus persisted parquet bars_20m_flow.parquet for future feature_build retrains. 230 tests pass; no src/ change. Wall: 3.1s.
2026-05-09 | 028 | H-192 | accept | master | n/a (regression check on round-015 CSCV) | **H-192 CSCV PBO refinement REGRESSION PASSED**: cross-strategy PBO=0.000000 (matches round-015 to 0.000e+00), 12,870 combinations, S=16 partitions, chunk_size=1967, median_logit=+2.197, median rel-rank=0.900 (IS-best lands at the 90th percentile of OOS ranks on average). Modal IS-best=baseline_offline_tau (12,853 / 12,870 = 99.87%). Per-strategy IS-best counts: [baseline_offline_tau 12853, baseline_online_tau 17, conformal_gate_tau 0, mondrian_aci_size 0, null_random_at_rate 0]. The four non-baseline-offline strategies essentially never win IS, confirming round-015's verdict that the offline layer alone dominates. | scripts/round_028_h192_cscv.py: rebuilds returns matrix at c_stop=ALPHA from cached predictions + 5 strategies; runs cscv_pbo with S=16 (Bailey et al. default); produces RESEARCH/diagrams/round_028/h192_cscv_panel.png (PBO histogram + relative-rank distribution + modal IS-best counts), h192_summary.json, h192_per_partition.csv, headline.json. Wall: 17.4s. Procedural standardisation: any future backtest round must report cscv_pbo with S=16 protocol (BACKLOG H-192 spawning rule). 230 tests pass.
2026-05-09 | 029 | H-181 | accept | master | n/a (SHAP on persisted offline model + round-021 trade dataframe) | **H-181 falsifier PASSES with massive headroom**: 466 of 726 features (64%) significant at BH-FDR alpha=0.01 between SHAP-at-win and SHAP-at-loss subsets of baseline_offline_tau trades (n_win=4,178; n_loss=3,216). Top-10 features by |SHAP diff|: (1) return_rolling_mean_8 (SHAP +0.046 win vs +0.005 loss; p=1e-57), (2) return_rolling_mean_12 (+0.037 vs +0.005; p=1e-28), (3) return_rolling_mean_2 (+0.012 vs -0.001), (4) body (+0.010 vs -0.003), (5) return_rolling_min_2, (6) oc_return, (7) return_rolling_mean_4, (8) return_rolling_min_1, (9) return_rolling_mean_1, (10) minute_sin (+0.013 win vs +0.019 loss — REVERSE direction; consistent with round-021 hour-of-day finding). **Diagnostic interpretation**: rolling-mean-return features dominate; model relies heavily on momentum signal — when momentum aligns with realized direction the trade wins. Conversely, certain time-of-day cycles (minute_sin) push the model toward false signals. This pinpoints where the offline model is most miscalibrated — momentum interpretation in specific hour clusters. Feeds Phase D feature prioritisation (H-130/131/132 ordering): focus on directional features that DON'T duplicate rolling-mean returns. | scripts/round_029_h181_shap_at_loss.py: loads round-021 trade_postmortem.parquet, filters baseline_offline_tau (n=7,394 trades), pulls entry features from bars_20m_features.parquet, computes SHAP via model.get_feature_importance(type='ShapValues'), Mann-Whitney U + BH-FDR over 726 features. Custom benjamini_hochberg helper. Outputs under RESEARCH/diagrams/round_029/: shap_at_loss_full.csv (726 rows), shap_at_loss_top15.png, headline.json. Wall: 9.1s (SHAP itself was 2.0s). 230 tests pass.
2026-05-09 | 030 | H-182 | accept | master | n/a (post-hoc cell analysis on round-021 trade dataframe + new Hurst feature) | **MAJOR FINDING**: baseline_offline_tau has +0.61 per-bar Sharpe in low-vol low-Hurst cell (CI [+0.37, +0.96]; n=102; BH-FDR significant). All 3 LOW-parkinson-var cells positive: pk=0/h=0 +0.61, pk=0/h=1 +0.53, pk=0/h=2 +0.50. **6 of 9 (pk × hurst) cells significant after BH-FDR α=0.05.** Most negative cell is pk=2/h=2 (high vol + high Hurst): Sharpe -0.07 [-0.11, -0.03]. **The model HAS tradable alpha but only conditional on low parkinson_var.** The +0.0078 marginal per-bar Sharpe from round 020 was a noisy mix of +0.5 in low-vol cells and ~0 to -0.07 in mid/high-vol cells. **Actionable**: regime-conditional gating on parkinson_var tercile (low-vol only) should produce a tradable strategy. Future rounds: (a) build low-vol-only strategy variant; (b) walk-forward H-150 to verify the cells are stable across refit windows. | scripts/round_030_h182_conditional_sharpe.py: filters round-021 trades to baseline_offline_tau, fits parkinson_var × Hurst train-only terciles, assigns each trade to a 3×3 cell, computes per-cell Sharpe with stationary-block-bootstrap CI (using src/bootstrap.py from round 020), BH-FDR over 9 cells, p-value via 2 × min(P(samples ≤ 0), P(samples ≥ 0)). Hurst computed via compute_hurst_rs(window=128) from src/features.py. Outputs under RESEARCH/diagrams/round_030/: h182_per_cell_sharpe.csv (9 rows), h182_panel.png (Sharpe heatmap + bar chart with CIs), headline.json. Wall: 215s (Hurst on full feats took ~3 min). 230 tests pass.
2026-05-09 | 031 | low-vol-gate (Phase A follow-up) | accept | master | n/a (regime-gated variant of round-017 baseline_offline_tau) | **TRADABLE STRATEGY EMERGES**: Round-030's H-182 finding directly exploited via train-only parkinson_var tercile gate. low_vol_only variant: annualized Sharpe = +20.86, per-bar Sharpe = +0.0489 with 95% block-bootstrap CI [+0.0371, +0.0597] (CI EXCLUDES ZERO), n_trades = 325, walk-forward Sharpe = +21.48 ± 7.30 across 5 folds (t-stat ≈ 6.6), p_boot = 0.000 vs shuffled-signal null. low_plus_mid_vol (excludes top tercile only): annualized Sharpe = +4.48, per-bar = +0.0350 [+0.0238, +0.0469] CI excludes zero, n_trades = 2,795, WF = +4.47 ± 2.65 — MORE practical strategy with 8.6× more trades and still strongly positive. high_vol_only is clearly NEGATIVE: ann_Sharpe = -1.58, per-bar = -0.016 [-0.026, -0.005] CI excludes zero (negative), n=4,702, WF = -1.32 ± 1.67 — confirms the toxicity of high-vol regime. Cross-variant CSCV PBO = 0.000, modal IS-best = low_vol_only across 12,870 combinations. Equity overlay (RESEARCH/diagrams/round_031/low_vol_gated_equity.png): green (low_vol) climbs steadily to +0.50; navy (low+mid) climbs to +1.05; gray (ungated) noisy ~+0.45; red (high_vol) monotone -0.75. **The signal is real: model has alpha conditional on low parkinson_var; high-vol regime is where it loses.** | scripts/round_031_low_vol_gate.py: gates baseline_offline_tau (tau=0.20, c_stop=inf) by  (or q67 / >= q67 variants). Train-only edges: q33=2.86e-6, q67=7.50e-6. Test pk distribution: 33.7% < q33, 69.6% < q67 (test slice is mildly higher-vol than train, expected). Walk-forward 5-fold + bootstrap-null + CSCV-PBO + per-variant block-bootstrap CI. Outputs under RESEARCH/diagrams/round_031/: low_vol_gated_variants.csv (4 variants × 17 metrics), low_vol_gated_equity.png, per_variant_sharpe_cis.png, headline.json. Wall: 354s. **Caveats**: post-hoc selection of vol-tercile (informed by round 030's 9-cell BH-FDR-significant analysis); the gate was NOT pre-registered. Future rounds should run walk-forward H-150 to verify regime stability across refit windows + verify on a held-out future slice (no such slice exists yet — Phase D follow-up). Also: the +20.87 annualized Sharpe is conservative because n=325 trades; the more practical low_plus_mid_vol +4.48 is the actionable headline. 230 tests pass.
