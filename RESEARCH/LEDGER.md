# Experiment Ledger

Append-only. One line per round. The status report.

Schema (pipe-separated):
```
date | round | hypothesis-id | status | branch | mlflow_run | primary_metric_delta | one_line_note
```

`status` ∈ {accept, iterate, kill, bootstrap, housekeeper}.

---

2026-05-09 | 000 | H-bootstrap | bootstrap | agent/round-000-bootstrap | n/a | n/a | Loop infrastructure ported from barrier_classifier sibling: RESEARCH/ governance, scripts (loop_daemon.ps1, loop_status.py, critic_check.py, compact_loop_state.py, run_round.py, audit_chart.py), generic src modules (uncertainty.py, conformal.py, mlflow_utils.py, training_helpers.py, backtest.py), 24 architecture-agnostic tests passing. Project-specific tests parked in tests/_pending/ for re-enable as utilities land in H-101..H-103. BACKLOG re-anchored to offline+online system; H-101 (port labels+splits) is top priority.
2026-05-09 | 001 | H-005 | accept | agent/round-001-h005-first-pnl | barrier_round_001 | offline τ=0.30 Sharpe=+1.50 PSR=0.995 p_boot=0.000; final τ=0.20 Sharpe=-4.69 p_boot=0.99 | First PnL number for online_barrier_classifier. Glue-only round: load predictions.parquet + 1m + bars_20m, map open_time→minute_idx (CODE-SCOUT), drop bars whose [n_k+1,n_k+M] crosses segment gap, run simulate_inventory_aware on τ_open∈{0.10,0.15,0.20,0.30,0.50} for {p_offline,p_final=p_online} with symmetric φ=c_stop=α=0.0041113 and 1bp/side cost. Shuffled-signal random-entry null (n=200) per Bailey/Borwein/LdP 2014 — drift-matched, addresses BTC2024 +120% drift. Result: ALL metrics finite (primary falsifier passes). p_offline has p_boot<0.05 at every τ (best at τ=0.30 with PSR=0.995); p_final/p_online has no detectable edge above τ=0.10. Online layer's ROC-trade-for-coverage cost shows up as flat-or-worse Sharpe — consistent with CONSTITUTION I (online primary metric is coverage, not Sharpe). Followups: H-005b (val-split τ_open selection), H-005c (CSCV PBO post-hoc τ deflation, blocked on H-113). Loop infra fixes also in commit: critic_check uses master not main, causality test check tolerates _pending state, .gitignore excludes pycache, conformal property test slack 3σ→4σ.
2026-05-09 | 002 | H-201 | accept | agent/round-002-h201-coverage-baseline | n/a (no MLflow this round; deterministic measurement) | Mondrian LAC max\|gap\|@α=0.10 = +0.034 (within 5pp); naive-threshold max\|gap\|@α=0.10 = +0.099 (way over); Mondrian @α=0.20 low-vol gap = -7.9pp on p_online (the 'gap to close' for H-202..H-208) | Coverage baseline on existing ARF + offline predictions. Pure measurement round; no model retrain (seed_noise_band=0). Glue-only: load predictions.parquet + bars_20m_features tail, derive parkinson_var_rolling_mean_24 terciles, chronological 30/70 cal/eval split (n_cal=9446, n_eval=22040). For predictors {p_offline, p_online} × α∈{0.05,0.10,0.20} compute three modes: naive_threshold (set={y:p≥α}, no calibration; the 'as-if-conformal' baseline H-201 names), lac_marginal (split-conformal global q_hat per Manokhin Ch.3), lac_mondrian (per-tercile q_hat per Manokhin Ch.11). Plus constant-predictor sanity: at α=0.05 cov=1.0000 ✓, at α=0.20 cov=0.9181 = 1-eval_base_rate ✓ (harness-falsifier passes per THEORIST). Findings: (1) naive uncalibrated p≥α over-covers by 8-10pp at α=0.10 in every regime — confirming raw ARF probabilities are NOT directly usable as conformal thresholds. (2) marginal LAC has +3.3pp under-coverage on high-vol p_online @α=0.10 vs -7.9pp over-coverage on low-vol @α=0.20 — exact conditional miscalibration THEORIST predicted. (3) Mondrian LAC compresses the α=0.10 gap to ±3.4pp everywhere but at α=0.20 still has -7.9pp gap on low-vol/p_online and -5.8pp on low-vol/p_offline — these are the gaps H-202 ACI / H-203 Mondrian-ACI must close. Spawned: H-201b (finer regime binning at α=0.20 — quintiles on parkinson_var, see whether higher-resolution Mondrian closes the low-vol gap further), H-201c (compare gap at fixed α=0.10 across cal-fraction ∈ {20%,30%,40%,50%} to check sensitivity to cal-window size).
2026-05-09 | 003 | H-108 | accept | master | n/a (engineering port; no model training on real data) | port verified: 10/10 ensemble tests pass; on N=3 synthetic-fit seeds median seed-disagreement std=0.017, p95=0.036, max single-model deviation from ensemble = 0.070 → averaging is non-trivial | CatBoostEnsemble class ported from sibling barrier_classifier/src/utils.py to src/ensemble.py. Two extensions over sibling: load_ensemble(base_path, n_models) classmethod for the round-trip the autonomous loop needs (sibling has save but not load), plus __len__/n_models for ergonomics. Tests cover: empty-list rejection, len/n_models, predict_proba averaging matches manual mean (rtol=1e-10), proba rows sum to 1, predict thresholding monotone in τ, feature_importance averaging, get_best_iteration with eval_set, save+load roundtrip with all shards present, load_ensemble rejects n_models<1 and missing shards. Diagnostic plot RESEARCH/diagrams/round_003/ensemble_dispersion.png shows seed-disagreement histogram + scatter vs ensemble p — read back via Read tool, caption corrected after first render to match what data actually shows (no U-shape; broadly distributed). Unblocks H-206 (CatBoost virtual-ensemble σ_epistemic as conformal feature). No notebook integration in this round — that's a follow-up if needed; the class is drop-in compatible with the current `models[i].fit(...)` pattern in offline_train.ipynb.
2026-05-09 | 004 | H-101 | accept | master | n/a (engineering port; deterministic round-trip validation) | round-trip on persisted bars_20m_features.parquet (n=78,714): chronological_split utility produces train_end=37,783, val_end=47,228 — bit-identical to notebook cell-3; 21/21 label+split tests pass | Extracted label+split logic from notebooks/feature_build.ipynb cell 5 and notebooks/offline_train.ipynb cell 3 into src/utils.py: compute_log_excursion (next-bar high / current close, NaN at segment gaps), calibrate_alpha (90%-quantile of train-window excursions, with the notebook's `int(N*tf)-1` slice — the `-1` matters and is pinned by test), construct_labels (1[excursion ≥ α]), chronological_split (no embargo, no walk-forward — matches this project's contract), chronological_split_indices (helper exposing the boundary math; pins the notebook's two-step int() truncation `val_size = int(int(N*tf)*vf)` which differs from int(N*tf*vf) by ≤1). Tests pin: pointwise log_excursion on hand-checked 7-bar synthetic with one segment gap; gap-crossing label = NaN; series-end label = NaN; alpha calibration matches manual quantile; alpha rejects invalid train_fraction/quantile; split partition disjoint+ordered; split returns independent copies. Round-trip test loads the actual parquet and compares my utility's split sizes/boundaries against a verbatim re-implementation of cell 3 — match exact. Retired parked tests/_pending/test_causality.py + test_splits.py: their sibling-imported semantics (chronological_split_with_embargo, walk_forward_cv, minute-bar construct_labels with C/eta params, compute_past_target_features) don't match this project; replacement test file is tests/test_label_split_utils.py. Diagnostic plot RESEARCH/diagrams/round_004/split_window_visualization.png shows train/val/test on date axis with per-window p+ (0.097/0.113/0.097) and rolling positive rate by window — first render had corrupted x-axis (Binance microsecond timestamps mis-parsed as ns); fixed by routing through src.utils.infer_unix_timestamp_unit before re-rendering. Unblocks H-105 (NSGA-II HPO needs reusable splits).
2026-05-09 | 005 | H-106 | accept | master | n/a (engineering port + measurement on already-fit predictions) | offline ECE 0.050/0.103/0.171 (low/med/high parkinson_var_24 terciles); online ECE 0.015/0.014/0.016 (regime-flat). Global: offline ECE=0.108 Brier=0.090; online ECE=0.015 Brier=0.076. Direct empirical proof of CONSTITUTION I (online trades ranking for regime-conditional calibration). | Ported four helpers from sibling barrier_classifier/src/utils.py to this project's src/utils.py: expected_calibration_error (10-equal-width-bin, last-bin-closed-on-right, sklearn-compatible), compute_all_metrics (roc_auc + pr_auc + log_loss + brier + ECE), calibration_by_regime (pd.qcut(n_regimes) with default low/med/high labels for 3 regimes; min_samples_per_regime=50 drops noisy buckets; reports n_samples + base_rate + ECE + Brier + mean_predicted per regime), threshold_analysis (101-point sweep returning n_trades/trade_rate/precision/recall, monotone in trade_rate). 13 new tests in tests/test_calibration_metrics.py pass: ECE=0 on perfect calibration, ECE=|p−base_rate| on constant predictor, p=1.0 included in last bin (boundary case), compute_all_metrics returns documented keys all-finite, calibration_by_regime n_samples sum to N, undersized-bucket dropping, label/n_regimes mismatch rejected, threshold_analysis monotone-decreasing in n_trades, precision matches manual at sample threshold, NaN precision when n_trades=0. Diagnostic RESEARCH/diagrams/round_005/calibration_by_regime.png — 2×3 calibration curves on the actual test stream (n=31,486) with parkinson_var_rolling_mean_24 terciles. Top row (offline) shows systematic over-prediction at every regime, severity scaling with volatility (mean_p − base_rate: 0.072−0.022=0.050 in low, 0.366−0.195=0.171 in high); bottom row (online ARF) shows curves on the diagonal in every regime (mean_p ≈ base_rate per regime). Plus threshold_sweep.csv and headline.json with full per-regime metric blocks. Latent bug fixed: src/training_helpers.py:204 referenced bc_utils.compute_all_metrics which didn't exist — now does. Unblocks any round that wants regime-stratified accept-gates per CONSTITUTION IV.
2026-05-09 | 006 | H-107 | accept | master | n/a (engineering port; deterministic visual diagnostic) | 13/13 plotting smoke tests pass; visual report card on real data reproduces round-005 ECE numbers exactly (offline 0.108, online 0.015) — helpers wired correctly | Ported four plot helpers from sibling barrier_classifier/src/utils.py into a NEW src/plotting.py module (cleaner than sibling's all-in-utils arrangement): plot_calibration_curve (reliability diagram with ECE annotation; ax composable), plot_calibration_by_regime (side-by-side per-quantile; matches src.utils.calibration_by_regime semantics; n_regimes parameter beyond sibling's hardcoded 3), plot_feature_importance (API improved: takes importances ARRAY not model object → works with CatBoostEnsemble / sklearn / anything; rejects misaligned lengths; top_n auto-caps to array length), plot_threshold_curves (reads output of src.utils.threshold_analysis; rejects missing columns). Weight plotters (plot_weight_profiles, plot_weight_distributions) intentionally NOT ported — no sample weights in this project until H-102, would test against stub data which is worse than no test. Tests pin: each helper returns a usable Axes/Figure; titles + axis labels populated; ECE annotation toggleable; ax composability (caller passes their own ax); custom labels and n_regimes mismatch rejected. Diagnostic RESEARCH/diagrams/round_006/visual_report_card.png is a 2×2 grid (offline + online calibration curves; top-25/726 feature importance horizontal bars; threshold sweep) plus separate per-regime calibration_by_regime PNGs (offline + online) reproducing round-005's parity exactly. Top features by offline CatBoost importance: minute_sin (4.02), return_rolling_mean_8 (3.19), return_rolling_mean_12 (2.69), minute_cos (2.11), parkinson_var_rolling_mean_2 (1.28) — time-of-day cyclic + short-window momentum + Parkinson vol dominate the model. Helpers are now drop-in for offline_train.ipynb / online_eval.ipynb (notebook integration deferred — reusable in any future round).
2026-05-09 | 007 | H-202 | accept | master | n/a (deterministic measurement; no model retrain) | ACI marginal coverage on real stream (n_eval=22,040, γ=0.01): 0.9508 / 0.9018 / 0.8023 (p_offline, α=0.05/0.10/0.20); 0.9505 / 0.9023 / 0.8023 (p_online). Marginal gap to target ≤ 0.003 (≤ 1.5σ, σ≈0.002). G&C 2021 Thm 1 marginal-coverage falsifier PASSES. | First true online conformal layer in this project: aci_step (one-step LAC update, q_{t+1}=clip(q_t+γ(err_t-α), 0, 1), set construction matches batch predict_set) and aci_stream (drives aci_step over a binary stream, returns sets/q_history/err_history/coverage). 14 new tests in tests/test_aci.py pin: per-step math (covered → q decreases by γα; miscovered → q increases by γ(1-α)); γ=0 freezes q; q clamps to [0,1]; set construction is parity with predict_set at fixed q; rejects invalid α/γ/y; stream shape parity with input; q_history starts at q_init; coverage converges within 5σ on stationary IID stream; γ scales q-volatility; rejects shape mismatch. Diagnostic RESEARCH/diagrams/round_007/ has three figures: aci_q_trajectory.png (q_t for both predictors at α=0.10 over 22,040 steps; both swing 0.05-0.80, p_online noisier than p_offline due to score-distribution shifts), aci_coverage_convergence.png (cumulative coverage converges cleanly to the 0.90 target line by step ~5000 for both predictors), aci_per_regime_vs_round002.png (signed coverage gap, target-empirical, ACI vs round-002 Mondrian-LAC at α=0.10 and 0.20). Per-regime finding: ACI's single global q_t cannot match Mondrian's per-regime q_hat — at α=0.10 ACI has -6.4pp low-vol over-coverage and +5.9pp high-vol under-coverage on p_offline (Mondrian: +3.4pp / -0.5pp); at α=0.20 ACI gaps are -9.3/+9.6pp vs Mondrian -5.8/-1.0pp. Plain ACI matches ARF/lac_marginal-level performance (better than naive_threshold's 8-10pp gaps) but loses per-regime to Mondrian. This is the expected story under non-stationary regime drift and is exactly what H-203 (Mondrian-ACI hybrid: per-regime q_t with the same online update rule) is designed to close.
2026-05-09 | 008 | H-203 | accept | master | n/a (deterministic measurement; no model retrain) | Mondrian-ACI per-regime gap collapses to ≤0.6pp everywhere; beats round-002 Mondrian-LAC; the α=0.20 low-vol p_online gap that round-002 named as "the gap to close" closes from -7.9pp to +0.04pp. Marginal coverage stays on target. | Mondrian-ACI = plain-ACI lifted to per-regime q_t: aci_mondrian_step (looks up active regime's q, runs aci_step, returns updated regime->q dict; rejects unknown regime; doesn't mutate caller's dict) + aci_mondrian_stream (drives per-regime q over a stream with regime labels, returns sets/q_history/q_history_by_regime/err_history/coverage/regimes_seen). 9 new tests in tests/test_aci_mondrian.py pin: only-active-regime updates; returns independent dict (no caller mutation); rejects unknown regimes; **bit-exact reduction to plain aci_stream when n_regimes=1** (load-bearing); per-regime q_history lengths sum to T; per-regime coverage converges within 6σ on stationary IID stream; q_init_by_regime overrides default; rejects shape mismatch; integer regime labels work. Full suite: 113 passed. On real stream (n_cal=9,446 used per-regime to warm q_init, n_eval=22,040, γ=0.01, terciles fit on full df): plain ACI per-regime gap range ±[5.8pp, 10.4pp] → Mondrian-ACI per-regime gap range ±[0.04pp, 0.54pp]. Examples at α=0.10 / p_offline: low-vol -5.8pp → +0.04pp (Δ improvement 5.76pp); high-vol +6.2pp → -0.21pp (Δ 6.00pp). At α=0.20 / p_online (the canonical 'gap to close'): low-vol -4.1pp → +0.04pp; high-vol +7.4pp → -0.54pp. Comparison vs round-002 batch Mondrian-LAC: Mondrian-ACI BEATS Mondrian-LAC across the board — at α=0.10/p_offline Mondrian-LAC was +3.4pp low-vol/-0.5pp high-vol; Mondrian-ACI is +0.04pp/-0.21pp. The R002 baseline used a held-out cal set + batch quantile; Mondrian-ACI uses per-regime online updates with γ=0.01 and per-regime warmed q_init; the online formulation handles regime-specific score distribution drift better than a static per-regime quantile. Diagnostic RESEARCH/diagrams/round_008/three_way_gap_comparison.png shows ACI/Mondrian-ACI/Mondrian-LAC bars at α=0.10 and 0.20 — Mondrian-ACI bars sit on the 0 line, others scatter ±5-10pp. Per-regime q trajectories plot (per_regime_q_trajectories.png) shows Mondrian-ACI's per-regime q stays near regime-specific levels (low-vol q stays low, high-vol q stays high) while plain ACI's global q oscillates in a wider band. Falsifier "Mondrian-ACI per-regime gap ≤ 5pp on every regime at α=0.10" PASSES with massive headroom (max gap 0.21pp). H-204 (River ARF vs SRP vs HAT under coverage-as-metric) and H-205 (locally-weighted conformal) become the next per-regime refinements; H-207 (ADWIN drift detector triggers ACI threshold reset) is now most natural to add on top of Mondrian-ACI.
2026-05-09 | 009 | H-005b | accept | master | n/a (no model retrain; uses persisted model.cbm to predict on val) | val-chosen tau* = 0.44 (inside R001 grid {0.10..0.50}); test Sharpe at tau* = -0.089 (PSR=0.458, p_boot=0.000 vs shuffled-signal null mean -3.57, DSR=0.000 after 26-tau-grid deflation); R001's PSR=0.995 at τ=0.30 was a post-hoc artifact and is hereby DEMOTED to "exploratory only". | scripts/round_009_val_tau.py loads artifacts/offline_model/model.cbm (726-feature CatBoost; selected_features.json was top-120 by importance, NOT a training restriction) and predicts p_offline on the chronological val slice (n=9,445 from chronological_split utility, all kept after gap-filter). Val tau-grid sweep on {0.10, 0.12, …, 0.60} (26 points, step 0.02) using simulate_inventory_aware with the round-001 setup (φ=c_stop=α=0.0041113, M=20, cost_bps=1.0); pick winner by max val Sharpe with n_trades>=50 stability filter. tau* = 0.44 (val Sharpe +0.347, n_trades 1,292 — many τ in val show NEGATIVE Sharpe, the +0.347 peak at τ=0.44 is the only positive island). Apply tau* to test predictions (existing artifacts/online_eval/predictions.parquet, gap-filter-aligned): test n_trades=3,427, total log return -0.024, max drawdown 0.361, hit rate 0.525. Bootstrap null (n=200, shuffled-signal random-entry per Bailey-Borwein-LdP 2014, drift-matched): null Sharpe mean -3.57, std 0.60, p_boot=0.000 → observed -0.089 dominates the null (model has SOME information, beats random-at-same-trade-rate). Deflated Sharpe (Bailey-LdP 2014, n_trials=26, var_trial=val sweep variance, n_obs=3,427, skew=0, kurt=3): DSR=0.000 (no edge survives multiple-comparisons deflation), SR_star=2.68 (the threshold that 26 zero-skill trials would need to beat). Plots: val_tau_sweep.png (val Sharpe-vs-τ curve with red dashed line at tau*; most of the grid is negative — the +0.347 peak at 0.44 is narrow and noisy), test_at_val_tau_equity.png (cumulative log return oscillates around zero, hits -25% drawdown midway through test, recovers partially, ends near zero). Honest summary: model has discrimination (it beats random-entry-at-same-rate), no tradable alpha at this label / cost / barrier-strategy combination after honest τ selection. Plus loop-infra: scripts/build_report.py + RESEARCH/REPORT.md — canonical living dashboard that pulls from every round's headline.json each iteration; trading section leads (per user direction); regenerated by Phase 6 of every round per LOOP_DISCIPLINE.md. Spawns: H-024 (re-evaluate label/strategy coupling now that the round-001 alpha is gone — first-touch label or no-stop backtest could change the picture), H-025 (try sized entries based on Mondrian-ACI confidence — does the conformal layer + sizing rule unlock something the binary p≥τ rule can't?). Both queued; user direction will pick.
2026-05-09 | 011 | phase-A-r1 | accept | master | n/a (no model retrain; uses persisted model.cbm + ARFClassifier replay) | baseline_offline_tau τ*=0.44 test Sharpe=-0.089 n=3,427 p_boot=0.000 (reproduces R009 sanity floor); baseline_online_tau τ*=0.52 test Sharpe=-11.59 n=289 p_boot=1.000 (online clusters near base rate; τ=0.52 is deep tail, the few entries are bad); combined_avg_tau τ*=0.18 test Sharpe=-0.116 n=7,207 p_boot=0.000 (50/50 averaging hurts: more trades, slightly worse Sharpe than offline alone). DSR within-strategy=0 for all three. | MANDATE OVERRIDE supersedes BACKLOG: Phase A round 1 lands the unified prediction surface and the first 3 of 7 Phase-A strategies. New code: src/inference.py (RegimeCuts dataclass + fit_regime_cuts on TRAIN-ONLY parkinson_var_rolling_mean_24 terciles; warm_q_init_by_regime per α from val LAC quantiles; predict() returning the §A.1 surface — p_offline, p_online, regime_id, q_lo_{05,10,20}, in_set_{05,10,20}, sigma_epistemic, confidence_score), src/strategies.py (StrategyOutput dataclass + 3 round-1 strategies in registry — baseline_offline_tau, baseline_online_tau, combined_avg_tau). 13 inference tests + 16 strategy tests pin: regime cuts boundary semantics + balanced fit; warm_q_init returns finite q in [0,1] with fallback for under-sampled regimes; predict() emits all mandated columns with finite q histories, in_set ∈ {0,1}, non-negative confidence; strategy registry contracts. River had numpy-2 ABI mismatch (river 0.21 → 0.24.2 fixes it). Round runner scripts/phase_A_round_011.py: chronological-split (train/val/test = 37,783/9,445/31,486); replay ARFClassifier prequentially over (last 96 train bars warmup) + val to materialise val p_online (n=9,445 cached at artifacts/phase_A/val_predictions_with_online.parquet); fit regime terciles on train (edges 2.86e-06 / 7.50e-06); warm Mondrian-ACI per-regime q on val LAC at α∈{0.05,0.10,0.20}; build unified val + test prediction tables via predict(); for each of the 3 strategies sweep τ∈{0.10..0.60 step 0.02} on val Sharpe with min_n_trades=50, evaluate at val-chosen τ on test, run shuffled-signal bootstrap null (n=200), within-strategy DSR. Diagrams under RESEARCH/diagrams/phase_A/round_011/: equity_overlay_baselines.png shows green (offline) hovering near 0, blue (online, almost no trades) drifting to -20%, red (combined-avg, 7k trades) hitting -42% drawdown around bar 12.5k. Reproduction: baseline_offline_tau hits R009's exact numbers (τ*=0.44, test Sharpe=-0.089, n=3,427) — the harness is bit-equivalent. Cross-strategy DSR (with proper n_trials = 3 strategies × 26 τ-grid = 78) deferred to round 013 when all 7 strategies are in. Wall clock: 285s. Phase A round 2 (strategies 4-7) is next.
2026-05-09 | 012 | phase-A-r2 | accept | master | n/a (no model retrain; uses round-011 unified prediction parquets) | All 7 §A.2 strategies under unified harness with 5-fold walk-forward + multi-strategy DSR (n_trials=137, var_trial=9.40). All strategies' multi-strategy DSR=0; strongest test Sharpe is offline_tau (-0.089). Stacked val Sharpe (-0.016) was best on val but stacker over-fits: test Sharpe collapses to -1.07. Conformal gate p_boot=0.39 (noise region). Mondrian-ACI sized k*=20 p_boot=0.49 (no edge). Walk-forward bars: offline mean +3.53±6.40 — std bigger than mean → not statistically distinguishable from zero across folds. | scripts/phase_A_round_012.py reads round-011's val/test unified prediction parquets and runs the remaining strategies (combined_stacked_tau via fit_stacker on val LR; conformal_gate_tau gates open at α=0.10 in_set; mondrian_aci_size sizes by clip(k*confidence_score) at α=0.10; null_random_at_rate at offline-baseline's empirical 0.123 entry rate, single-point grid). Adds to src/backtest.py: simulate_inventory_aware_sized (cost on full position, PnL × size, 7 tests including unit-size-equivalence + half-size-halves-pnl + zero-size-skips + size out-of-range rejection + inventory-cap preserved + c_stop=∞ disables SL); walk_forward_backtest (5 contiguous folds default; 4 tests); cscv_pbo (López de Prado 2014 Ch.11 PBO via combinatorial OOS rank logits; 4 tests including pure-noise PBO band 0.35..0.75 and clear-winner PBO<0.05). Adds to src/strategies.py: combined_stacked_tau + fit_stacker (sklearn LogisticRegression on val), conformal_gate_tau (in_set_{α} gate with α-level switchable), mondrian_aci_size (size = clip(k * max(0, p_off - (1-q_lo)), 0, 1)), null_random_at_rate (deterministic seed; rejects out-of-range target_rate). 169 tests passing; sklearn deprecation warning is library-internal (scipy L-BFGS-B disp/iprint deprecation in scipy 1.18). Fitted stacker: coef_offline=4.42, coef_online=1.16, intercept=-3.57 — heavily weights offline; online's marginal contribution (1.16) is small but positive. The val Sharpe peaking at -0.016 for the stacked model and then test Sharpe collapsing to -1.07 is the textbook overfitting signal. Diagrams under RESEARCH/diagrams/phase_A/round_012/: equity_overlay_round2.png (7 lines, all negative or near-zero on test, online crashes by bar 8k), walk_forward_sharpe_bars.png (mean ± std bars; std > mean for every non-null model), val_tau_sweep_round2.png (val sweep across knob axes). Phase A round 3 (CSCV pass + report) is next. Wall clock: 167s.
2026-05-09 | 014 | planning-014 | infra | master | n/a (research-planning round; no code shipped) | 13 new H-IDs (H-301, H-302, H-303, H-304, H-305, H-306, H-310-a, H-310-b, H-311, H-312, H-313, H-320-a, H-320-b) + 15 inline refinements; 3 genuine gaps surfaced (rolling-retrain ×2 = H-310; trade postmortem = H-306; per-metric bootstrap ×2 = H-320); 9 web-only references catalogued in INDEX.md under new [STATS-CI] tag (DeLong 1988, Sun-Xu 2014, Boyd-Eng-Page 2013, Politis-Romano 1994, Politis-White 2004, Niculescu-Mizil-Caruana 2005, Roelofs 2022) plus Malinin 2021 + Geifman-El-Yaniv 2017 + Montiel 2021 (River) + Gomes 2017 (ARF) + Gomes-Read-Bifet 2019 (SRP) + Bifet-Gavaldà 2009 (HAT). Synthesis: RESEARCH/research_plan_round_014.md ≤ 600 lines with 8-ask→H-ID map, anti-slop check (round 009/011/012 numbers), and concrete next-8-rounds ordering (H-024 → H-320-a → H-320-b → H-310-a → H-310-b → H-306 → H-302 → H-305). | Read CONSTITUTION I/IV/V.b + LOOP_DISCIPLINE + ROUND_TEMPLATE + AGENTS + full BACKLOG + last 15 LEDGER + KILL_LIST + INDEX.md + src/{uncertainty,conformal,ensemble,backtest,utils}.py + REPORT.md + last 3 round headlines. Anti-slop concrete numbers: R009 test Sharpe=-0.089 n=3,427; R011 baselines 1-3 reproduce R009 exactly + online catastrophic (-11.59) + combined-avg slightly worse than offline (-0.116); R012 all 7 strategies multi-strategy DSR=0 with n_trials=137 var_trial_pooled=9.40, combined_stacked val=-0.016 → test=-1.07 (textbook overfit), conformal_gate p_boot=0.39 (noise region), mondrian_aci_size p_boot=0.49 (no edge). Canonical "gap to close": no strategy survives deflated bar; three axes attackable — strategy (H-024 no-stop / H-023 first-touch), features (H-111/112/114/115 + H-311/312/313), process (H-310 rolling retrain). Spawned LIT-SCOUT agents in parallel (3) for INDEX-flagged gaps; integrated their citations into H-302, H-305, H-320-a/b card References. Sub-agent contracts honored: local PDF first per AGENTS.md, web-fetch only for explicit gaps. CONSTITUTION V.b strategy-realism honored in every strategy card (val-knob selection, inventory cap, cost, stop or explicit no-stop, max-DD circuit-breaker). Multiple-comparisons correction named for every sweep card (H-302 Bailey-Borwein 130-trial DSR + CSCV; H-303 Bonferroni at α=0.05/27 + CSCV; H-310-b paired block-bootstrap on ΔSharpe; H-320-a per-metric scheme). Predicted effects above seed-noise band on every effect-claiming card. Phase A r2 commit (round 012) and any prior round-013 partial work are not part of this round's deliverables — round-014 is the planning interlude per user mandate; round-013 partial artefacts (scripts/phase_A_round_013.py, RESEARCH/diagrams/phase_A/round_013/, .tmp/round_013.log) remain untracked for future pickup. No round-NNN-accepted tag.
